name: hermes-id
version: 1.5.0
description: "Self-Sovereign Identity — Ed25519 identity cards, mutual auth handshake, HTTP auth server with agent registry, MCP server, and admin CLI. Every instance gets a unique DID and signed credential."
pip_dependencies: []
